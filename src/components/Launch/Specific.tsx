import React from 'react'
import {IndexDetail} from '../LaunchDetail/index'

export const Specific = (data:any) => {
    console.log(data)
    var id=data.toString();
    return(
        // <IndexDetail id={id}/>
        <div>
            
        </div>
    )
}
